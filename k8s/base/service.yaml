apiVersion: v1
kind: Service
metadata:
  name: agent-boilerplate
  labels:
    app.kubernetes.io/name: agent-boilerplate
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: agent-boilerplate
