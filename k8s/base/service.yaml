apiVersion: v1
kind: Service
metadata:
  name: gargantua-parent
  labels:
    app.kubernetes.io/name: gargantua-parent
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: gargantua-parent
