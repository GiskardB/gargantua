apiVersion: apps/v1
kind: Deployment
metadata:
  name: gargantua-parent
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: agent
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 2Gi
