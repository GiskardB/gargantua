apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agent-dev

resources:
  - ../../base

patches:
  - path: patch-dev.yaml
    target:
      kind: Deployment
      name: agent-boilerplate
