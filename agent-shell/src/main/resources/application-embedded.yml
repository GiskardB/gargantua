# ══════════════════════════════════════════════════════════════════
# Embedded mode — no MongoDB, no Redis, everything in-memory
# Activate with: SPRING_PROFILES_ACTIVE=embedded
# ══════════════════════════════════════════════════════════════════

spring:
  autoconfigure:
    exclude:
      # MongoDB (Spring Boot 4.x package names)
      - org.springframework.boot.mongodb.autoconfigure.MongoAutoConfiguration
      - org.springframework.boot.data.mongodb.autoconfigure.DataMongoAutoConfiguration
      - org.springframework.boot.data.mongodb.autoconfigure.DataMongoRepositoriesAutoConfiguration
      # Redis (Spring Boot 4.x package names)
      - org.springframework.boot.data.redis.autoconfigure.DataRedisAutoConfiguration
      - org.springframework.boot.data.redis.autoconfigure.DataRedisRepositoriesAutoConfiguration
      # Gargantua memory auto-config (uses Redis/Mongo directly — replaced by EmbeddedProfileAutoConfiguration)
      - ai.gargantua.memory.autoconfigure.AgentMemoryAutoConfiguration

# Disable health checks for infra we don't have
management:
  health:
    mongo:
      enabled: false
    redis:
      enabled: false

logging:
  level:
    ai.gargantua: DEBUG
