package ${package};

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Main entry point for the ${agentName} agent, powered by the Gargantua AI Agent Framework. */
@SpringBootApplication
public class ${agentName}Application {

    public static void main(String[] args) {
        SpringApplication.run(${agentName}Application.class, args);
    }
}
